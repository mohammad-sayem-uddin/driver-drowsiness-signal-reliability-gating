opencv-python>=4.8.0,<5.0.0
mediapipe>=0.10.0,<0.10.30
numpy>=1.24.0,<2.0.0
pygame>=2.5.0,<3.0.0

# --- Dependency alignment (required for a runnable pipeline) ---
# mediapipe's legacy solutions API (FaceMesh) uses the older protobuf
# descriptor interface and breaks on protobuf >=5 ("FieldDescriptor has no
# attribute 'label'"). It must stay on protobuf 4.x. TensorFlow (optional
# CNN-ablation arm only) is pinned to 2.17.x, the last line that also accepts
# protobuf 4.x; newer TF (>=2.19) forces protobuf >=6 and is incompatible.
# jax/jaxlib must NOT be installed: TF imports jax optionally and guards the
# ImportError, but a mismatched jax/ml_dtypes pair crashes that import.
protobuf>=4.25.3,<5
tensorflow==2.17.1
