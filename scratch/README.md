# scratch/

Ad-hoc developer probes. **Non-authoritative.** The real verification suites are
in `tests/` (`tests/test_suite.py`, `tests/smoke_test.py`,
`tests/test_event_metrics.py`). Nothing here is cited by any report or the paper.

`archive/` holds the four root-level `test_*.py` probe scripts moved out of the
repository root during the 2026-07-30 cleanup, where their `test_*.py` names
risked being mistaken for the real suite (and swept up by test discovery). They
are kept for provenance only — see `Repository_Cleanup/ARCHIVE_PLAN.md` §5.
